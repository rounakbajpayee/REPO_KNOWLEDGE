# MCP
mcp>=1.0.0

# Qdrant — pinned to match server version 1.9.x
# Do not upgrade until server is upgraded
qdrant-client>=1.9.0,<1.10.0

# HTTP (Ollama calls)
httpx>=0.27.0

# CLI
click>=8.1.0

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
