# Qdrant (Tailscale)
QDRANT_URL=http://100.70.3.86:6333

# Ollama (Tailscale)
OLLAMA_URL=http://100.70.3.86:11434

# Embedding model — nomic-embed-text (768-dim) or qwen3-embed (1024-dim)
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_DIM=768

# Qdrant collection name — includes model slug to support benchmarking across models
# Format: code_chunks_{model_slug}
QDRANT_COLLECTION=code_chunks_nomic

# Root directory to scan for repositories
PROJECTS_ROOT=/Users/rounak/Projects

# How many chunks to return per search
SEARCH_TOP_K=5
